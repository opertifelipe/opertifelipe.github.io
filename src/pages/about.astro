---
import { getCollection, render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const document = (await getCollection('pages')).find(({ data }) => data.path === 'about.md');
if (!document) throw new Error('About document not found.');
const { Content } = await render(document);
---

<BaseLayout title={document.data.title} description={document.data.description}>
  <article class="document document--about">
    <Content />
  </article>
</BaseLayout>
